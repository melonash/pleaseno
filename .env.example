# Server-side only. Get one at https://console.typesafe.ai
TYPESAFE_API_KEY=

# Any long random string. Signs the hidden game state so players cannot edit the meter.
# Generate with: openssl rand -hex 32
GAME_STATE_SECRET=
